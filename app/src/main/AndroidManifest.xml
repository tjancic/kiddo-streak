<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name="com.kiddostreak.app.KiddoStreakApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.KiddoStreak">

        <activity
            android:name="com.kiddostreak.app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.KiddoStreak">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.kiddostreak.feature.widget.StreakWidgetTinyReceiver"
            android:exported="true"
            android:label="Streak (1×1)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/streak_widget_info_tiny" />
        </receiver>

        <receiver
            android:name="com.kiddostreak.feature.widget.StreakWidgetSmallReceiver"
            android:exported="true"
            android:label="Streak (Small)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/streak_widget_info_small" />
        </receiver>

        <receiver
            android:name="com.kiddostreak.feature.widget.StreakWidgetMediumReceiver"
            android:exported="true"
            android:label="Streak (Medium)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/streak_widget_info_medium" />
        </receiver>

        <receiver
            android:name="com.kiddostreak.core.notifications.BootCompletedReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
